// Generated by CoffeeScript 1.12.3
(function() {
  this.Mutation = (function() {
    function Mutation() {}

    Mutation.muation = function(arr) {
      var comparisonArray, element, elementsLetters, i, j, k, l, len, len1, len2, lettersArray, lettersOfWordsArray, m, ref, returnValue;
      returnValue = true;
      arr = arr.map(function(value) {
        return value.toLowerCase();
      });
      comparisonArray = [];
      ref = arr[0];
      for (k = 0, len = ref.length; k < len; k++) {
        j = ref[k];
        if (comparisonArray.indexOf(j) < 0) {
          comparisonArray.push(j);
        }
      }
      lettersOfWordsArray = [];
      for (l = 0, len1 = arr.length; l < len1; l++) {
        element = arr[l];
        elementsLetters = 0;
        lettersArray = [];
        for (m = 0, len2 = element.length; m < len2; m++) {
          i = element[m];
          if (comparisonArray.indexOf(i) < 0) {
            returnValue = false;
          }
        }
      }
      return console.log(returnValue);
    };

    Mutation.muation(['hello', 'Hello', 'lol']);

    return Mutation;

  })();

  $(function() {
    var mutations;
    return mutations = new Mutation();
  });

}).call(this);
